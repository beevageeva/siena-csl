
<h1><%= t('student_assign_list' , :coursename => Course.find(params[:course_id]).name   ) %></h1>
  <%= form_tag({:controller => 'student_assigns' , :action => 'unregister_from_course'}, {:method => :post}) do |f| %>
    <%= hidden_field_tag("course_id"  , params[:course_id]) %>
		
<%= grid(@students, :show_filters => :always) do |st|

   st.action_column :param_name => "studentids"	


    st.column :name => t('name') , :model => "User" , :attribute => "name" do |s|
            s.user.name
    end

    st.column  :name => t('username') , :model => "User" , :attribute => "username" do |s|
      s.user.username
    end


end %>

<%= submit_tag t("delete_from_course") %>
<% end %>


<br/>


    <%= link_to t('student_assign_list_not' , :coursename => Course.find(params[:course_id]).name   ) , :controller => "student_assigns" , :action => "listByCourseNot" , :course_id => params[:course_id]%>
<br/>


		<br/>
    <%= link_to t('back'), :controller => "courses" , :action => "show" , :id => params[:course_id]%>
