<b> <%= t('alu_group_list') %></b>
<%= grid(@alu_groups, :show_filters => :always) do |st|
    st.column :name => t('name')  do |s|
            s.name
    end
    st.column :name => t('competence_list') do |s|
			link_to t("competence_list") , :action => 'listByGroup', :alu_group_id => s.student_alu_groups[0].alu_group_id
		end


end %>
